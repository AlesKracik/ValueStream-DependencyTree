* eventually add past customer issues including link to jira and resolution
* import assign featureless epics to some feature, that is wrong
* after import of epics from jira, epics that are not assigned to a feature should visible, but not linked to any feature. also we need to revork assigning epics to features as now it possible to assign existing epic, not just create a new one.
• add buttons to canvas to reset view, zoom to fit, etc.
• lock the highlight
• maintenance, performace or techdebt linked to existing TCV? to "keep it"
• autoadd customer existing TCV to interrupts?
• introduce time series to TCV
* add team location, to properly adjust sprint capacity (which assumes 10 working days per sprint)
