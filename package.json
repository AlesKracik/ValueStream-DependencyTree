{
  "name": "valuestream-dependencytree",
  "version": "1.0.0",
  "description": "ValueStream Dependency Tree - Monorepo root",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix web-client\"",
    "build:shared": "npm run build --prefix shared/types",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix web-client",
    "build": "npm run build:shared && npm run build:backend && npm run build:frontend",
    "test": "npm run test --prefix backend && npm run test --prefix web-client",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix web-client"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
